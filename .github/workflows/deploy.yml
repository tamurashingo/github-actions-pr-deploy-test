name: deploy

on:
  push:
    branches:
      - main

permissions:
  pull-requests: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        run: echo 'checkout ok'
      - name: deploy
        run: echo 'deploy ok'

